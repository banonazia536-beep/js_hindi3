 /* 
=========================
1} constant and variable 
=========================
A] constant:- we can have one type to define constant by the help of const keyword . 
B] variable:- we can have two type to define variable in js
     1. by the help of var 
     2. by the help of let 
  => for define variable in js mostly we can use "let" not var in node js .
  => var is mostly use in browser 
  */
const student_id = 101;
let a = "nazia";
var subject ="javascript";
console.log (student_id);
console.log(a);
console.log(subject);
/* => some time variable is denife without any ketword like */
n = 'mohan';
console.log(n);

